import { MiActiveDirective } from './mi-active.directive';

describe('MiActiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MiActiveDirective();
    expect(directive).toBeTruthy();
  });
});
