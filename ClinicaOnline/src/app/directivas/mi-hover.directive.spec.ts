import { MiHoverDirective } from './mi-hover.directive';

describe('MiHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new MiHoverDirective();
    expect(directive).toBeTruthy();
  });
});
